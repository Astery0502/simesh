# 独立应用任务与当前任务保留事项

日期：2026-09-09。面向开发协作。

三个分支已完成并按用户要求整合到当前分支。以下分工保留为历史范围记录，
整合与 simplify 结果见[整合记录](feature-integration.md)。

用户决定在新的任务及独立 worktree 中并行开发以下三项，以当前已验证的应用层为共同基线。
任务的初始消息记录具体基线提交。各自遵守 `analysis-core/AGENTS.md`，生产实现与用户文档使用英文，
开发协作使用中文；不修改主工作区或父包的其他改动。

| 独立任务 | 主要落点 | 本轮范围 |
| --- | --- | --- |
| 原生 AMR 积分与统计 | `analysis-core/src/simesh/reductions.py`、独立测试与文档 | 原始叶块内部值的体积积分、加权统计、矩形选区/表面通量 |
| 显式 MHD 热力学恢复 | `analysis-core/src/simesh/physics/mhd.py`、独立测试与文档 | 明确模型与单位的速度、热压、温度及相关常用量 |
| 应用结果保存与恢复 | `analysis-core/src/simesh/results_io.py`、独立测试与文档 | 几何、图、射线结果与紧凑轨迹的版本化、安全读写 |

独立任务优先新增自己的模块，不同时改 `__init__.py`、`applications.py`、`geometry.py`、
全局 README 和当前方向文档。通过模块导入即可使用，公共导出及便利入口在后续集成时统一处理。
确有必要改共享契约时，先说明具体阻塞和最小变化，不自行扩大为通用执行框架。
各任务完成实现与相称验证后，提交本 worktree 的改动，报告提交号、测试和已知限制；不自动合并或推送。

共同保持：原生核心仍为非周期笛卡尔三维；QSL/twist 诊断不保存积分路径；
筛选种子后再独立追踪；应用追踪默认双向。已有一般数组接口继续保留。

## 后续已整合：字段组合与沿线诊断

共同基线为 `4f6fc30`。两个独立任务均显式指定 `gpt-6-astra`、`high`，
完成后提交各自 worktree，不自动合并或推送。

| 独立任务 | worktree / 分支 | 范围 |
| --- | --- | --- |
| 完善准备场的字段组合接口 | `75cd/simesh` / `codex/field-composition` | 准备后分量子集、重排、重命名、合并及多输出逐点派生，保持生命周期与值来源约定 |
| 实现带弧长与编号的沿线诊断 | `0d13/simesh` / `codex/line-profiles` | 基于现有 LineSet 与采样接口的编号、分支、几何弧长、数值及有效状态；不依赖字段组合任务未合并代码 |

worktree 均位于 `/Users/astery/.codex/worktrees/` 下，已确认从共同基线启动。
两个功能提交 `af731d0`、`6e602cf` 已按用户后续要求无冲突整合，并完成三路 simplify。
结果及验证见[本轮整合记录](composition-profile-integration.md)；原工作区保持干净并保留。
当前任务继续分析[大数据内存峰值及路径](memory-workflows.md)，没有把该探索分配给其他任务。

## 仍留在当前任务分析

- 时间序列与多快照流程。
- 真实强 QSL、长磁力线、弱场/零点邻域的验证。
- 散度、力平衡等数值质量汇总。
- 独立追踪与 QSL 脚点的定位一致性。
- 真实大输入、应用级流式输出和负载均衡。

这些事项没有交给功能开发任务，不视为它们必须顺带完成的工作。
