# 字段组合与沿线诊断整合

日期：2026-09-09。面向开发协作。

以 `4f6fc30` 为共同基线，无冲突汇总两个独立 worktree 提交：

- `af731d0`：字段选择、合并与多输出逐点派生。
- `6e602cf`：带全局种子编号、分支弧长与采样状态的沿线剖面。

两个独立任务均指定 Astra high；原分支与 worktree 保留，没有推送。
本次合并保留功能提交历史，父包既有未提交改动未纳入。

## 公共接入

顶层 `simesh` 导出 `select_fields`、`merge_fields`、`derive_many`、
`sample_line_profiles`、`LineProfiles` 和 `LineProfile`。
原来的 `derive`、原生采样与追踪接口保持可用。

更新 README、两份功能说明与联合指南；扩展既有联合示例及集成测试，打通：
MHD 恢复 → 多输出单位换算 → 与电流合并 → 沿瞬时速度流线采样。
集成测试也对已保存再恢复的曲线采样，分别记录曲线和采样场来源。

字段选择、合并与派生仍独立拥有结果，并使用新的值身份。
旧 curl 证明不能穿过字段重排/复制；先确定 B 组，再计算并复用其 curl。
剖面保留两份种子及原始点索引，几何弧长不解释为时间或精确 ODE 长度。
`LineProfiles` 尚未注册进结果文件 schema；现有保存格式继续只接受明确列出的类型。

## simplify 处理

按 `/Users/astery/.codex/skills/simplify/SKILL.md` 进行三路审查，
复用、质量、效率审查均使用 Astra high 并阅读完整汇总差异。

- 将字段组合与剖面的同一套分量解析合并到 `fields._component_indices`。
  它只解析选择，不强制共同单位，保留混合物理量剖面的合法用法。
- 补足组合发布时的临时叶目录、掩码和去重索引预算。
  配方临时结果在发布前已释放，因此两阶段临时容量取最大值。
- 剖面展示只复制本种子的两个终止状态，避免独立单条剖面通过小视图保留整组状态数组。
  两状态的 16 字节也纳入展示预算；普通分支的数值数组仍可共享只读视图。

没有引入零复制模式、通用执行器或新的插值实现。

## 验证

- 首轮组合、剖面、原派生与联合测试：60 项通过。
- 最终 `analysis-core/make test`：**266 项通过、2 项可选 OpenMP 跳过**。
- 新增回归覆盖三种组合入口在发布临时空间不足时提前拒绝，以及独立剖面不保留父数组。
- 联合示例通过：质量 `2.4e-12 kg`、质量加权温度 `840000 K`、底面外向磁通
  `-2e-4 T*m^2`；6 条双向速度流线，240 个温度/密度/电流采样点全部可用。
- 解析温度比较避开常值物理边界延拓区；没有用解析外推替换既定边界处理。
- 没有 Cython 改动，无需重建扩展；差异空白检查通过。

大数据方向仍是讨论阶段。[内存探索](memory-workflows.md)已补充整合后的典型场景与方法保留，
没有把字段组合或 point_batch 描述成自动限制全流程内存的机制。
