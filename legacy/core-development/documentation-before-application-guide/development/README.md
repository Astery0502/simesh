# 核心与应用开发记录

面向开发协作。选定实现已从 `analysis-core/` 提升到根目录 `src/simesh/`，
构建、测试和示例也从仓库根目录运行。N4 数据、数值与所有权边界保留。

| 需要了解的内容 | 入口 |
| --- | --- |
| 当前方向与选定实现 | [当前状态](current.md) |
| 固定的数据、数值与所有权边界 | [核心设计](next-generation-design.md) |
| 应用开发方式与验证范围 | [应用开发](application-development.md) |
| 当前 Python 接口 | [项目说明](../../../../README.md) |
| 物理能力、输出格式和示例产物 | [交付清单](../capabilities-and-outputs.md) |
| 目录、导入路径与兼容接口变更 | [迁移说明](../../../../MIGRATION.md) |
| 两代历史实现的区别 | [历史目录说明](../../../README.md) |

普通应用工作从目标结果出发，组合已有字段与消费者，执行代表性流程和受影响检查。
只有具体缺陷、能力阻塞或实测问题才触发核心改动；不默认重启核心重构或完整性能矩阵。

本目录的早期记录保留当时版本、工作区路径和测试证据。记录中出现的
`analysis-core/`、旧根目录 `src/` 或 `rewrite/` 路径应按当时语境理解，
不作为当前运行入口。旧代码分别位于 `legacy/previous/`、`legacy/python-first/`，
历史核心比较脚本与本机产物位于 `legacy/core-development/`。

更早的规范、阶段实现和比较见[历史资料](archive/README.md)，
它们不是当前任务清单或应用开发的必经流程。
